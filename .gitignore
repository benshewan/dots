# Devenv
.devenv*
devenv.local.nix
devenv.local.yaml

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml

# Nix pkg build
/result

# sops / age: never commit decryption keys or plaintext secrets
*.key
keys.txt
secrets/**/*.plain.*
secrets/**/*.decrypted.*
